import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sensorymapper.app',
  appName: 'Sensory Mapper',
  webDir: '.next/server/app'
};

export default config;
